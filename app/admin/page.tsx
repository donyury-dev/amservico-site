import { AdminPanel } from "@/components/admin-panel";

export const metadata = {
  title: "Painel Administrativo | AM Serviço e Manutenção",
};

export default function AdminPage() {
  return <AdminPanel />;
}
